apiVersion: v1
kind: Service
metadata:
  name: dpi-engine-service
  labels:
    app: dpi-engine
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: dpi-engine
